/** Activity / audit log — placeholder until the feature is implemented. */
export function LogsPage() {
  return (
    <section className="placeholder">
      <h2>Activity Logs</h2>
      <p>Coming soon — who created, updated or deleted records, and when.</p>
    </section>
  );
}
